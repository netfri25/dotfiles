(defwidget window-name []
  (tooltip
    (label :text {current-window-name?.title})
    (label
      :text {current-window-name?.title}
      :truncate true
      :class "text"
    )
  )
)

(deflisten current-window-name :initial "{}" "ristate -w")
